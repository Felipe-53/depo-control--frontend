import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({

  title: {
    marginTop: '5%',
  },

  btnContainer: {
    marginTop: '15%',
    width: '80%',
  },

  optionBtn: {
    height: '3.5rem',
  },
});

export default useStyles;